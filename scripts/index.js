console.log(`There are ${characters.length} characters in the array.`);

console.log(characters[0]);

// characters.forEach(function(character){
//     console.log(`Charactre name: ${character.name}`);
// })